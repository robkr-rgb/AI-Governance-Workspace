# Source

Origin: `mattpocock/skills`, `skills/engineering/grill-with-docs`

Imported: 2026-05-27

Reason: Adds a documented planning workflow that challenges project plans against repo language, captures domain terms in `CONTEXT.md`, and records durable architectural decisions in `docs/adr/`.

Policy:

- Git-backed copy in this workspace is the source of truth for Rob's installation.
- Install into `/Users/rob/.codex/skills/grill-with-docs` from this directory.
- Review upstream changes before applying them; do not auto-sync external edits into the runtime.
