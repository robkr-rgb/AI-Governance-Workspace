# Source

Origin: `mattpocock/skills`, `skills/productivity/caveman`

Imported: 2026-05-27

Reason: Adds an opt-in ultra-compressed communication mode for concise technical work when Rob asks for terse output.

Policy:

- Git-backed copy in this workspace is the source of truth for Rob's installation.
- Install into `/Users/rob/.codex/skills/caveman` from this directory.
- Use only when explicitly triggered by Rob with phrases such as `caveman mode`, `use caveman`, `less tokens`, `be brief`, or `/caveman`.
- Review upstream changes before applying them; do not auto-sync external edits into the runtime.
